# 素材目录

- `干净原图/`：清晰图片，用于合成退化样张（有"标准答案"可算 PSNR/SSIM）。**纳入版本库**。
- `合成老照片/`：用 `代码/utils/degrade.py` 从干净原图合成的"假老照片"。可重生成，**不纳入版本库**。
- `真实老照片/`：真实扫描的老照片。**隐私，不纳入版本库**（.gitignore 已排除）。
- `风格图/`：VGG 自定义艺术风格迁移使用的参考图，包含暖调家庭记忆风格等。**纳入版本库**。
